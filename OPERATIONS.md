# 運用・保守ガイド (OPERATIONS)

本書は Human Silhouette Detector を運用・保守する担当者向けの手引きです。
日常運用、設定変更、トラブルシュート、将来拡張（Phase 2）の進め方をまとめています。

---

## 1. 起動と停止

### 起動（Web 配信・主たる運用形態）
```bash
source .venv/bin/activate        # Windows: .venv\Scripts\activate
python app.py
```
ブラウザで `http://<サーバIP>:5000` を開く。同一マシンなら `http://localhost:5000`。

### 起動（ローカル確認・補助）
```bash
python main.py    # 'q' キーで終了
```

### 停止
`app.py` を実行中のターミナルで `Ctrl + C`。背景スレッドは daemon のためプロセス終了で停止する。

---

## 2. 設定変更（config.py）

コードを触らず `config.py` の値だけで挙動を調整できる設計です。主な項目:

| 目的 | キー | 調整の方向 |
|---|---|---|
| 動作が重い | frame_width / frame_height | 下げると軽くなる（例 480×360） |
| 動作が重い | num_poses | 減らすと軽くなる |
| 検出が過敏 / 鈍い | min_pose_detection_confidence | 上げると厳しく、下げると緩く |
| 輪郭の見え方 | outline_color / outline_thickness / fill_alpha | 色・太さ・透過度 |
| 人数表示の要否 | show_person_count | True / False |
| 配信画質と帯域 | jpeg_quality | 下げると軽い・粗い |
| 使用カメラ | camera_index | 複数カメラ時に変更 |
| 待受ポート | port | 既定 5000 |

変更後はアプリを再起動すると反映される。

---

## 3. トラブルシュート

| 症状 | 主な原因 | 対処 |
|---|---|---|
| 画面が「Camera not available」 | カメラ未接続 / 使用中 / index 違い | 接続確認、他アプリ終了、camera_index を変更 |
| 起動時にモデル関連エラー | .task 未配置 / パス違い | models/README.md に従い配置、model_path 確認 |
| 映像がカクつく・遅い | CPU 性能不足 | 解像度・num_poses を下げる（第 2 章） |
| 映像が固まったまま | カメラ供給停止 | カメラ再接続、アプリ再起動 |
| ブラウザに何も出ない | ポート / ファイアウォール | port 確認、ファイアウォールで許可 |
| 他端末から見られない | host 設定 / ネットワーク | host=0.0.0.0 を確認、同一 LAN か確認 |
| 輪郭が出ない（人物はいる） | しきい値が高い | mask_threshold / 検出信頼度を下げる |

---

## 4. 依存・ライセンス保守

- 依存パッケージを更新したら、`THIRD_PARTY_LICENSES.md` の記載と実際のライセンスを再突合する。
- 商用組込前提のため、寛容ライセンス（Apache 2.0 / BSD / MIT）以外（特に AGPL）の混入は禁止。
- 確認コマンド: `pip freeze` で版を控え、新規依存があればライセンスを調査して一覧へ追記する。

---

## 5. 既知の制約

- 同時視聴は単一表示クライアントを想定。多数同時視聴は未対応（要件外）。
- 暗所・逆光・遮蔽が多い環境では検出精度が低下する（CV 一般の制約）。
- num_poses を超える人数は検出されない。設置環境の想定人数に合わせて調整する。

---

## 6. 将来拡張（Phase 2）の進め方

設計時に拡張点を用意済み。いずれも既存層を壊さず差し込める。

### 6.1 VLM 併用（映像→状況の文章説明）
1. 商用組込可ライセンスの VLM を選定する（**ライセンスの個別精査が必須**／リスク R-01）。
2. `src/recognition/base.py` の `RecognizerBase` を実装した VLM 認識クラスを追加する。
3. 用途に応じ、CV 認識と VLM 認識を切り替え / 併用する制御を加える。
4. CPU / iGPU で動くランタイム（例 ONNX Runtime 等）を基本設計で評価する。

### 6.2 サーバ転送 / 保存（顔データ含む）
1. **実装着手前に法務確認をゲートとする**（個人情報保護法等／リスク R-02）。
2. `src/output/base.py` の `OutputHandlerBase` を実装した転送 / 保存クラスを追加する。
3. 同意取得・利用目的明示・保持期間・暗号化・アクセス制御を有効化できる形で実装する。
4. `CameraWorker` 生成時に当該ハンドラを渡すだけで差し替わる（既存コード改修不要）。

### 6.3 拡張時の共通ルール
- 1 つの層の変更が他層に波及しないこと（疎結合の維持）を必ず確認する。
- 変更は変更管理履歴（PROJECT_DESIGN.md）に追記する。
- 追加・変更箇所はテスト仕様書（TEST_PLAN.md）にテストケースを追記して検証する。

---

## 7. 関連ドキュメント
- `README.md` — セットアップと実行
- `docs/PROJECT_DESIGN.md` — 要件定義〜テストの統合設計書
- `docs/TEST_PLAN.md` — テスト仕様書 兼 結果報告書
- `THIRD_PARTY_LICENSES.md` — 依存ライセンス一覧
- `models/README.md` — モデル取得・配置手順
